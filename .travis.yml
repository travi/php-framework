language: php
php:
  - 5.3
  - 5.4
  - 5.5

before_install:
  - npm install -g bower
  - npm install -g grunt-cli

install:
  - composer install
  - sh php/framework/test/php/mockProject/install-dependencies.sh

script: vendor/bin/phing

after_script: php vendor/bin/coveralls -v