build.dir=${basedir}
build.logs=${build.dir}/logs

building.dir=${build.dir}/site
dist.dir=${build.dir}/dist

site.dir=${build.dir}/..

src.dir=${framework.dir}/src

tests.dir=${framework.dir}/test

qunit.phpunit.runner.path=/home/travi/include/php/framework/test

jstd.conf=${shared.dir}/jsTestDriver.conf

server.path=/home/travi/deploy/framework/

autoload.path=${framework.dir}/vendor/autoload.php